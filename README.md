Sample deployment for a secure basic demo nginx container which runs non-privelidged.

The code uses a private harbor repo, so you will need to either pull the dockerfile and perform a docker build and port to your registry
